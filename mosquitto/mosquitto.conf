# Minimal local-dev config. Not production-hardened (no auth, no TLS) -
# revisit when we get to the real edge/MQTT integration stage.
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
log_dest stdout
